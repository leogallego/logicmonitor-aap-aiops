#!/usr/bin/python
# -*- coding: utf-8 -*-
# Auto-generated by AnsibleClaw from MCP tool: list_collector_versions
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)

from __future__ import absolute_import, annotations, division, print_function

__metaclass__ = type


DOCUMENTATION = r"""
---
module: list_collector_versions
short_description: List available collector versions in LogicMonitor (LM) monitoring. 

**Returns:** Array of collector versions with: version number, release date, stability level (GA/EA/RC), changelog summary, download size, platform support (Windows/Linux), mandatory/recommended flag. 

**What are collector versions:** Software releases for LogicMonitor collector agents. Collectors are installed on your infrastructure to gather monitoring data. Staying current ensures latest features, bug fixes, and security patches. 

**When to use:**
- Check for collector updates
- Review changelog before upgrading
- Find specific version for rollback
- Verify platform compatibility
- Plan maintenance windows for collector upgrades


**Version types:** 
- **GA (Generally Available):** Production-ready, stable, recommended 
- **EA (Early Adopter):** Beta, new features, use in non-production first 
- **RC (Release Candidate):** Pre-GA testing version 
- **Mandatory:** Critical security/bug fixes, upgrade required 

**Collector update workflow:** 1. Use this tool to check available versions 2. Review changelog for breaking changes 3. Test new version on non-production collector first 4. Use "get\_collector" to check current version on your collectors 5. Update collectors via LogicMonitor UI or API 6. Monitor collector health after upgrade 

**Version numbering:** Format is typically X.Y.Z (e.g., 34.100.0) where: 
- X = Major release (significant changes) 
- Y = Minor release (features, improvements) 
- Z = Patch release (bug fixes) 

**Best practices:** 
- Keep collectors within 2-3 versions of latest GA release 
- Subscribe to release notifications for critical updates 
- Test EA versions in lab before production 
- Upgrade during maintenance windows (may briefly interrupt monitoring) 
- Stagger upgrades (don't upgrade all collectors simultaneously) 

**Common scenarios:** 
- "Check if newer version available" → Compare latest version to your collectors 
- "Plan upgrade" → Review changelog, schedule maintenance 
- "Rollback needed" → Find previous stable version 
- "Platform migration" → Verify version supports new OS 

**Related tools:** "get\_collector" (check current version on collector), "list\_collectors" (find collectors to upgrade).
description:
  - List available collector versions in LogicMonitor (LM) monitoring. 

**Returns:** Array of collector versions with: version number, release date, stability level (GA/EA/RC), changelog summary, download size, platform support (Windows/Linux), mandatory/recommended flag. 

**What are collector versions:** Software releases for LogicMonitor collector agents. Collectors are installed on your infrastructure to gather monitoring data. Staying current ensures latest features, bug fixes, and security patches. 

**When to use:**
- Check for collector updates
- Review changelog before upgrading
- Find specific version for rollback
- Verify platform compatibility
- Plan maintenance windows for collector upgrades


**Version types:** 
- **GA (Generally Available):** Production-ready, stable, recommended 
- **EA (Early Adopter):** Beta, new features, use in non-production first 
- **RC (Release Candidate):** Pre-GA testing version 
- **Mandatory:** Critical security/bug fixes, upgrade required 

**Collector update workflow:** 1. Use this tool to check available versions 2. Review changelog for breaking changes 3. Test new version on non-production collector first 4. Use "get\_collector" to check current version on your collectors 5. Update collectors via LogicMonitor UI or API 6. Monitor collector health after upgrade 

**Version numbering:** Format is typically X.Y.Z (e.g., 34.100.0) where: 
- X = Major release (significant changes) 
- Y = Minor release (features, improvements) 
- Z = Patch release (bug fixes) 

**Best practices:** 
- Keep collectors within 2-3 versions of latest GA release 
- Subscribe to release notifications for critical updates 
- Test EA versions in lab before production 
- Upgrade during maintenance windows (may briefly interrupt monitoring) 
- Stagger upgrades (don't upgrade all collectors simultaneously) 

**Common scenarios:** 
- "Check if newer version available" → Compare latest version to your collectors 
- "Plan upgrade" → Review changelog, schedule maintenance 
- "Rollback needed" → Find previous stable version 
- "Platform migration" → Verify version supports new OS 

**Related tools:** "get\_collector" (check current version on collector), "list\_collectors" (find collectors to upgrade).
  - This module wraps the MCP tool 'list_collector_versions'.
  - Requires the C(ansible.mcp) collection and an MCP server connection.
version_added: "1.0.0"
author:
  - Auto-generated by AnsibleClaw
options:
  fields:
    description:
      - Comma-separated list of fields to include in response. Examples: "id,displayName,hostStatus" or use "*" for all fields. Omit this parameter to receive a curated set of commonly used fields.
    type: str
    required: false
  offset:
    description:
      - Starting offset for pagination (default: 0).
    type: float
    required: false
  size:
    description:
      - Number of results per page (default: 50, max: 1000).
    type: float
    required: false
seealso:
  - module: ansible.mcp.run_tool
    description: Generic MCP tool runner this module delegates to.
requirements:
  - ansible.mcp collection installed
  - MCP server accessible via ansible.mcp.mcp connection
"""

EXAMPLES = r"""
---
- name: List available collector versions in LogicMonitor (LM) monitoring. 

**Returns:** Array of collector versions with: version number, release date, stability level (GA/EA/RC), changelog summary, download size, platform support (Windows/Linux), mandatory/recommended flag. 

**What are collector versions:** Software releases for LogicMonitor collector agents. Collectors are installed on your infrastructure to gather monitoring data. Staying current ensures latest features, bug fixes, and security patches. 

**When to use:**
- Check for collector updates
- Review changelog before upgrading
- Find specific version for rollback
- Verify platform compatibility
- Plan maintenance windows for collector upgrades


**Version types:** 
- **GA (Generally Available):** Production-ready, stable, recommended 
- **EA (Early Adopter):** Beta, new features, use in non-production first 
- **RC (Release Candidate):** Pre-GA testing version 
- **Mandatory:** Critical security/bug fixes, upgrade required 

**Collector update workflow:** 1. Use this tool to check available versions 2. Review changelog for breaking changes 3. Test new version on non-production collector first 4. Use "get\_collector" to check current version on your collectors 5. Update collectors via LogicMonitor UI or API 6. Monitor collector health after upgrade 

**Version numbering:** Format is typically X.Y.Z (e.g., 34.100.0) where: 
- X = Major release (significant changes) 
- Y = Minor release (features, improvements) 
- Z = Patch release (bug fixes) 

**Best practices:** 
- Keep collectors within 2-3 versions of latest GA release 
- Subscribe to release notifications for critical updates 
- Test EA versions in lab before production 
- Upgrade during maintenance windows (may briefly interrupt monitoring) 
- Stagger upgrades (don't upgrade all collectors simultaneously) 

**Common scenarios:** 
- "Check if newer version available" → Compare latest version to your collectors 
- "Plan upgrade" → Review changelog, schedule maintenance 
- "Rollback needed" → Find previous stable version 
- "Platform migration" → Verify version supports new OS 

**Related tools:** "get\_collector" (check current version on collector), "list\_collectors" (find collectors to upgrade).
  hosts: mcp_servers
  connection: ansible.mcp.mcp
  gather_facts: false
  vars:
    ansible_mcp_server_name: "CHANGEME"
  tasks:
    - name: List available collector versions in LogicMonitor (LM) monitoring. 

**Returns:** Array of collector versions with: version number, release date, stability level (GA/EA/RC), changelog summary, download size, platform support (Windows/Linux), mandatory/recommended flag. 

**What are collector versions:** Software releases for LogicMonitor collector agents. Collectors are installed on your infrastructure to gather monitoring data. Staying current ensures latest features, bug fixes, and security patches. 

**When to use:**
- Check for collector updates
- Review changelog before upgrading
- Find specific version for rollback
- Verify platform compatibility
- Plan maintenance windows for collector upgrades


**Version types:** 
- **GA (Generally Available):** Production-ready, stable, recommended 
- **EA (Early Adopter):** Beta, new features, use in non-production first 
- **RC (Release Candidate):** Pre-GA testing version 
- **Mandatory:** Critical security/bug fixes, upgrade required 

**Collector update workflow:** 1. Use this tool to check available versions 2. Review changelog for breaking changes 3. Test new version on non-production collector first 4. Use "get\_collector" to check current version on your collectors 5. Update collectors via LogicMonitor UI or API 6. Monitor collector health after upgrade 

**Version numbering:** Format is typically X.Y.Z (e.g., 34.100.0) where: 
- X = Major release (significant changes) 
- Y = Minor release (features, improvements) 
- Z = Patch release (bug fixes) 

**Best practices:** 
- Keep collectors within 2-3 versions of latest GA release 
- Subscribe to release notifications for critical updates 
- Test EA versions in lab before production 
- Upgrade during maintenance windows (may briefly interrupt monitoring) 
- Stagger upgrades (don't upgrade all collectors simultaneously) 

**Common scenarios:** 
- "Check if newer version available" → Compare latest version to your collectors 
- "Plan upgrade" → Review changelog, schedule maintenance 
- "Rollback needed" → Find previous stable version 
- "Platform migration" → Verify version supports new OS 

**Related tools:** "get\_collector" (check current version on collector), "list\_collectors" (find collectors to upgrade).
      leogallego.logicmonitor_mcp.list_collector_versions:
      register: result
"""

RETURN = r"""
---
content:
  description: List of content blocks returned by the MCP tool.
  type: list
  returned: always
  elements: dict
is_error:
  description: Whether the MCP tool reported an error.
  type: bool
  returned: always
structured_content:
  description: Optional structured result data from the MCP tool.
  type: dict
  returned: when available
tool_name:
  description: Name of the MCP tool that was called.
  type: str
  returned: always
tool_classification:
  description: Tool classification information (read_only, destructive).
  type: dict
  returned: always
"""


MCP_TOOL_NAME = "list_collector_versions"


from ansible.module_utils.basic import AnsibleModule


def main():
    module = AnsibleModule(
        argument_spec=dict(
            fields=dict(
                type="str",
            ),
            offset=dict(
                type="float",
            ),
            size=dict(
                type="float",
            ),
        ),
        supports_check_mode=True,
    )

    if module.check_mode:
        module.exit_json(changed=False, msg="Check mode: no MCP call made")

    tool_args = {k: v for k, v in module.params.items() if v is not None}

    module.exit_json(
        changed=False,
        _mcp_delegate=dict(
            name=MCP_TOOL_NAME,
            args=tool_args,
        ),
    )


if __name__ == "__main__":
    main()
