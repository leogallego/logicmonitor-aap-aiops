#!/usr/bin/python
# -*- coding: utf-8 -*-
# Auto-generated by AnsibleClaw from MCP tool: get_role
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)

from __future__ import absolute_import, annotations, division, print_function

__metaclass__ = type


DOCUMENTATION = r"""
---
module: get_role
short_description: Get detailed information about a specific role by its ID in LogicMonitor (LM) monitoring. 

**Returns:** Complete role details: name, description, custom flag, detailed permission matrix (view/manage/delete/acknowledge for each area: resources/devices, alerts, dashboards, reports, settings, users). 

**When to use:** 
- Review exact permissions before assigning role
- Compare roles to choose correct one
- Document security policies
- Audit what a role can/cannot do
- Before creating custom role (use as template)

**Permission granularity returned:** 
- Resources: Can view/add/modify/delete resource/device
- Alerts: Can view/acknowledge/manage alert rules
- Dashboards: Can view/create/edit/delete dashboards
- Reports: Can view/create/schedule reports
- Settings: Can modify datasources/collectors/integrations
- Users: Can manage other users/roles

**Use cases:** 
- Security audit: "Can this role delete production resources/devices?"
- Least privilege: Choose role with minimal required permissions
- Documentation: Export role permissions for compliance
- Role comparison: Compare multiple roles to find right fit

**Workflow:** Use "list\_roles" to find roleId, then use this tool to review detailed permissions before assigning to users. 

**Related tools:** "list\_roles" (find roles), "list\_users" (see who has this role), "create\_role" (create custom role).
description:
  - Get detailed information about a specific role by its ID in LogicMonitor (LM) monitoring. 

**Returns:** Complete role details: name, description, custom flag, detailed permission matrix (view/manage/delete/acknowledge for each area: resources/devices, alerts, dashboards, reports, settings, users). 

**When to use:** 
- Review exact permissions before assigning role
- Compare roles to choose correct one
- Document security policies
- Audit what a role can/cannot do
- Before creating custom role (use as template)

**Permission granularity returned:** 
- Resources: Can view/add/modify/delete resource/device
- Alerts: Can view/acknowledge/manage alert rules
- Dashboards: Can view/create/edit/delete dashboards
- Reports: Can view/create/schedule reports
- Settings: Can modify datasources/collectors/integrations
- Users: Can manage other users/roles

**Use cases:** 
- Security audit: "Can this role delete production resources/devices?"
- Least privilege: Choose role with minimal required permissions
- Documentation: Export role permissions for compliance
- Role comparison: Compare multiple roles to find right fit

**Workflow:** Use "list\_roles" to find roleId, then use this tool to review detailed permissions before assigning to users. 

**Related tools:** "list\_roles" (find roles), "list\_users" (see who has this role), "create\_role" (create custom role).
  - This module wraps the MCP tool 'get_role'.
  - Requires the C(ansible.mcp) collection and an MCP server connection.
version_added: "1.0.0"
author:
  - Auto-generated by AnsibleClaw
options:
  roleId:
    description:
      - The ID of the role to retrieve
    type: float
    required: true
  fields:
    description:
      - Comma-separated list of fields to include in response. Examples: "id,displayName,hostStatus" or use "*" for all fields. Omit this parameter to receive a curated set of commonly used fields.
    type: str
    required: false
seealso:
  - module: ansible.mcp.run_tool
    description: Generic MCP tool runner this module delegates to.
requirements:
  - ansible.mcp collection installed
  - MCP server accessible via ansible.mcp.mcp connection
"""

EXAMPLES = r"""
---
- name: Get detailed information about a specific role by its ID in LogicMonitor (LM) monitoring. 

**Returns:** Complete role details: name, description, custom flag, detailed permission matrix (view/manage/delete/acknowledge for each area: resources/devices, alerts, dashboards, reports, settings, users). 

**When to use:** 
- Review exact permissions before assigning role
- Compare roles to choose correct one
- Document security policies
- Audit what a role can/cannot do
- Before creating custom role (use as template)

**Permission granularity returned:** 
- Resources: Can view/add/modify/delete resource/device
- Alerts: Can view/acknowledge/manage alert rules
- Dashboards: Can view/create/edit/delete dashboards
- Reports: Can view/create/schedule reports
- Settings: Can modify datasources/collectors/integrations
- Users: Can manage other users/roles

**Use cases:** 
- Security audit: "Can this role delete production resources/devices?"
- Least privilege: Choose role with minimal required permissions
- Documentation: Export role permissions for compliance
- Role comparison: Compare multiple roles to find right fit

**Workflow:** Use "list\_roles" to find roleId, then use this tool to review detailed permissions before assigning to users. 

**Related tools:** "list\_roles" (find roles), "list\_users" (see who has this role), "create\_role" (create custom role).
  hosts: mcp_servers
  connection: ansible.mcp.mcp
  gather_facts: false
  vars:
    ansible_mcp_server_name: "CHANGEME"
  tasks:
    - name: Get detailed information about a specific role by its ID in LogicMonitor (LM) monitoring. 

**Returns:** Complete role details: name, description, custom flag, detailed permission matrix (view/manage/delete/acknowledge for each area: resources/devices, alerts, dashboards, reports, settings, users). 

**When to use:** 
- Review exact permissions before assigning role
- Compare roles to choose correct one
- Document security policies
- Audit what a role can/cannot do
- Before creating custom role (use as template)

**Permission granularity returned:** 
- Resources: Can view/add/modify/delete resource/device
- Alerts: Can view/acknowledge/manage alert rules
- Dashboards: Can view/create/edit/delete dashboards
- Reports: Can view/create/schedule reports
- Settings: Can modify datasources/collectors/integrations
- Users: Can manage other users/roles

**Use cases:** 
- Security audit: "Can this role delete production resources/devices?"
- Least privilege: Choose role with minimal required permissions
- Documentation: Export role permissions for compliance
- Role comparison: Compare multiple roles to find right fit

**Workflow:** Use "list\_roles" to find roleId, then use this tool to review detailed permissions before assigning to users. 

**Related tools:** "list\_roles" (find roles), "list\_users" (see who has this role), "create\_role" (create custom role).
      leogallego.logicmonitor_mcp.get_role:
        roleId: "CHANGEME"
      register: result
"""

RETURN = r"""
---
content:
  description: List of content blocks returned by the MCP tool.
  type: list
  returned: always
  elements: dict
is_error:
  description: Whether the MCP tool reported an error.
  type: bool
  returned: always
structured_content:
  description: Optional structured result data from the MCP tool.
  type: dict
  returned: when available
tool_name:
  description: Name of the MCP tool that was called.
  type: str
  returned: always
tool_classification:
  description: Tool classification information (read_only, destructive).
  type: dict
  returned: always
"""


MCP_TOOL_NAME = "get_role"


from ansible.module_utils.basic import AnsibleModule


def main():
    module = AnsibleModule(
        argument_spec=dict(
            roleId=dict(
                type="float",
                required=True,
            ),
            fields=dict(
                type="str",
            ),
        ),
        supports_check_mode=True,
    )

    if module.check_mode:
        module.exit_json(changed=False, msg="Check mode: no MCP call made")

    tool_args = {k: v for k, v in module.params.items() if v is not None}

    module.exit_json(
        changed=False,
        _mcp_delegate=dict(
            name=MCP_TOOL_NAME,
            args=tool_args,
        ),
    )


if __name__ == "__main__":
    main()
