#!/usr/bin/python
# -*- coding: utf-8 -*-
# Auto-generated by AnsibleClaw from MCP tool: get_website
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)

from __future__ import absolute_import, annotations, division, print_function

__metaclass__ = type


DOCUMENTATION = r"""
---
module: get_website
short_description: Get detailed information about a specific website monitor by its ID in LogicMonitor (LM) monitoring. 

**Returns:** Complete website monitor details: name, type (webcheck/pingcheck), domain/URL, monitoring configuration, checkpoint locations, response time thresholds, SSL settings, authentication, custom headers, alert status. 

**When to use:** 
- Review monitoring configuration
- Check checkpoint locations
- Verify URL and settings
- Troubleshoot failed checks
- Audit SSL certificate monitoring

**Configuration details returned:** 
- steps: Multi-step transaction monitoring (for complex workflows)
- checkpoints: Which global locations perform checks (e.g., US-East, EU-West, Asia-Pacific)
- schema: HTTP vs HTTPS
- testLocation: Internal (from collector) vs External (from cloud)
- responseTimeThreshold: Alert if slower than X ms
- sslCertExpirationDays: Alert X days before cert expires

**Use cases:** 
- Verify website is monitored from correct geographic locations
- Check if SSL certificate expiration monitoring is enabled
- Review response time thresholds (too strict? too lenient?)
- Troubleshoot why website checks are failing
- Document what endpoints are monitored

**Workflow:** Use "list\_websites" to find websiteId, then use this tool for complete monitoring configuration. 

**Related tools:** "list\_websites" (find website), "update\_website" (modify), "generate\_website\_link" (get URL), "list\_website\_checkpoints" (available locations).
description:
  - Get detailed information about a specific website monitor by its ID in LogicMonitor (LM) monitoring. 

**Returns:** Complete website monitor details: name, type (webcheck/pingcheck), domain/URL, monitoring configuration, checkpoint locations, response time thresholds, SSL settings, authentication, custom headers, alert status. 

**When to use:** 
- Review monitoring configuration
- Check checkpoint locations
- Verify URL and settings
- Troubleshoot failed checks
- Audit SSL certificate monitoring

**Configuration details returned:** 
- steps: Multi-step transaction monitoring (for complex workflows)
- checkpoints: Which global locations perform checks (e.g., US-East, EU-West, Asia-Pacific)
- schema: HTTP vs HTTPS
- testLocation: Internal (from collector) vs External (from cloud)
- responseTimeThreshold: Alert if slower than X ms
- sslCertExpirationDays: Alert X days before cert expires

**Use cases:** 
- Verify website is monitored from correct geographic locations
- Check if SSL certificate expiration monitoring is enabled
- Review response time thresholds (too strict? too lenient?)
- Troubleshoot why website checks are failing
- Document what endpoints are monitored

**Workflow:** Use "list\_websites" to find websiteId, then use this tool for complete monitoring configuration. 

**Related tools:** "list\_websites" (find website), "update\_website" (modify), "generate\_website\_link" (get URL), "list\_website\_checkpoints" (available locations).
  - This module wraps the MCP tool 'get_website'.
  - Requires the C(ansible.mcp) collection and an MCP server connection.
version_added: "1.0.0"
author:
  - Auto-generated by AnsibleClaw
options:
  websiteId:
    description:
      - The ID of the website to retrieve
    type: float
    required: true
  fields:
    description:
      - Comma-separated list of fields to include in response. Examples: "id,displayName,hostStatus" or use "*" for all fields. Omit this parameter to receive a curated set of commonly used fields.
    type: str
    required: false
seealso:
  - module: ansible.mcp.run_tool
    description: Generic MCP tool runner this module delegates to.
requirements:
  - ansible.mcp collection installed
  - MCP server accessible via ansible.mcp.mcp connection
"""

EXAMPLES = r"""
---
- name: Get detailed information about a specific website monitor by its ID in LogicMonitor (LM) monitoring. 

**Returns:** Complete website monitor details: name, type (webcheck/pingcheck), domain/URL, monitoring configuration, checkpoint locations, response time thresholds, SSL settings, authentication, custom headers, alert status. 

**When to use:** 
- Review monitoring configuration
- Check checkpoint locations
- Verify URL and settings
- Troubleshoot failed checks
- Audit SSL certificate monitoring

**Configuration details returned:** 
- steps: Multi-step transaction monitoring (for complex workflows)
- checkpoints: Which global locations perform checks (e.g., US-East, EU-West, Asia-Pacific)
- schema: HTTP vs HTTPS
- testLocation: Internal (from collector) vs External (from cloud)
- responseTimeThreshold: Alert if slower than X ms
- sslCertExpirationDays: Alert X days before cert expires

**Use cases:** 
- Verify website is monitored from correct geographic locations
- Check if SSL certificate expiration monitoring is enabled
- Review response time thresholds (too strict? too lenient?)
- Troubleshoot why website checks are failing
- Document what endpoints are monitored

**Workflow:** Use "list\_websites" to find websiteId, then use this tool for complete monitoring configuration. 

**Related tools:** "list\_websites" (find website), "update\_website" (modify), "generate\_website\_link" (get URL), "list\_website\_checkpoints" (available locations).
  hosts: mcp_servers
  connection: ansible.mcp.mcp
  gather_facts: false
  vars:
    ansible_mcp_server_name: "CHANGEME"
  tasks:
    - name: Get detailed information about a specific website monitor by its ID in LogicMonitor (LM) monitoring. 

**Returns:** Complete website monitor details: name, type (webcheck/pingcheck), domain/URL, monitoring configuration, checkpoint locations, response time thresholds, SSL settings, authentication, custom headers, alert status. 

**When to use:** 
- Review monitoring configuration
- Check checkpoint locations
- Verify URL and settings
- Troubleshoot failed checks
- Audit SSL certificate monitoring

**Configuration details returned:** 
- steps: Multi-step transaction monitoring (for complex workflows)
- checkpoints: Which global locations perform checks (e.g., US-East, EU-West, Asia-Pacific)
- schema: HTTP vs HTTPS
- testLocation: Internal (from collector) vs External (from cloud)
- responseTimeThreshold: Alert if slower than X ms
- sslCertExpirationDays: Alert X days before cert expires

**Use cases:** 
- Verify website is monitored from correct geographic locations
- Check if SSL certificate expiration monitoring is enabled
- Review response time thresholds (too strict? too lenient?)
- Troubleshoot why website checks are failing
- Document what endpoints are monitored

**Workflow:** Use "list\_websites" to find websiteId, then use this tool for complete monitoring configuration. 

**Related tools:** "list\_websites" (find website), "update\_website" (modify), "generate\_website\_link" (get URL), "list\_website\_checkpoints" (available locations).
      leogallego.logicmonitor_mcp.get_website:
        websiteId: "CHANGEME"
      register: result
"""

RETURN = r"""
---
content:
  description: List of content blocks returned by the MCP tool.
  type: list
  returned: always
  elements: dict
is_error:
  description: Whether the MCP tool reported an error.
  type: bool
  returned: always
structured_content:
  description: Optional structured result data from the MCP tool.
  type: dict
  returned: when available
tool_name:
  description: Name of the MCP tool that was called.
  type: str
  returned: always
tool_classification:
  description: Tool classification information (read_only, destructive).
  type: dict
  returned: always
"""


MCP_TOOL_NAME = "get_website"


from ansible.module_utils.basic import AnsibleModule


def main():
    module = AnsibleModule(
        argument_spec=dict(
            websiteId=dict(
                type="float",
                required=True,
            ),
            fields=dict(
                type="str",
            ),
        ),
        supports_check_mode=True,
    )

    if module.check_mode:
        module.exit_json(changed=False, msg="Check mode: no MCP call made")

    tool_args = {k: v for k, v in module.params.items() if v is not None}

    module.exit_json(
        changed=False,
        _mcp_delegate=dict(
            name=MCP_TOOL_NAME,
            args=tool_args,
        ),
    )


if __name__ == "__main__":
    main()
