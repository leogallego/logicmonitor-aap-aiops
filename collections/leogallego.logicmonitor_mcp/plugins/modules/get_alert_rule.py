#!/usr/bin/python
# -*- coding: utf-8 -*-
# Auto-generated by AnsibleClaw from MCP tool: get_alert_rule
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)

from __future__ import absolute_import, annotations, division, print_function

__metaclass__ = type


DOCUMENTATION = r"""
---
module: get_alert_rule
short_description: Get detailed information about a specific alert rule by ID in LogicMonitor (LM) monitoring. 

**Returns:** Complete alert rule details: name, priority, enabled status, detailed matching conditions (device groups, datasources, datapoints, instance filters, severity levels), escalation chain assignment, suppression windows, notification settings. 

**When to use:**
- Review exact matching logic before modifying rule
- Troubleshoot why alert matched (or didn't match) this rule
- Document alert routing policies
- Verify suppression settings
- Check which escalation chain receives matching alerts


**Matching conditions explained:** 
- deviceGroups: Which resource/device folders this rule applies to (e.g., /Production/, /Database Servers/) 
- datasources: Which datasources trigger this rule (e.g., CPU, Memory, AWS_EC2) 
- datapoints: Specific metrics (e.g., CPUBusyPercent, MemoryUsedPercent) 
- instances: Filter by instance name (e.g., C: drive only, eth0 interface only) 
- severity: Alert levels (critical, error, warn) 
- escalatingChainId: Where matching alerts are routed 

**Troubleshooting use cases:** 
- "Why did this CPU alert go to wrong team?" → Check resource/device group + datasource filters 
- "Why didn't I get paged?" → Verify alert matches conditions AND check escalation chain 
- "Too many alerts" → Review if conditions too broad, add instance filters 

**Workflow:** Use "list\_alert\_rules" to find ruleId, then use this tool to review complete matching logic and routing. 

**Related tools:** "list\_alert\_rules" (find rules), "update\_alert\_rule" (modify), "get\_escalation\_chain" (check notification chain).
description:
  - Get detailed information about a specific alert rule by ID in LogicMonitor (LM) monitoring. 

**Returns:** Complete alert rule details: name, priority, enabled status, detailed matching conditions (device groups, datasources, datapoints, instance filters, severity levels), escalation chain assignment, suppression windows, notification settings. 

**When to use:**
- Review exact matching logic before modifying rule
- Troubleshoot why alert matched (or didn't match) this rule
- Document alert routing policies
- Verify suppression settings
- Check which escalation chain receives matching alerts


**Matching conditions explained:** 
- deviceGroups: Which resource/device folders this rule applies to (e.g., /Production/, /Database Servers/) 
- datasources: Which datasources trigger this rule (e.g., CPU, Memory, AWS_EC2) 
- datapoints: Specific metrics (e.g., CPUBusyPercent, MemoryUsedPercent) 
- instances: Filter by instance name (e.g., C: drive only, eth0 interface only) 
- severity: Alert levels (critical, error, warn) 
- escalatingChainId: Where matching alerts are routed 

**Troubleshooting use cases:** 
- "Why did this CPU alert go to wrong team?" → Check resource/device group + datasource filters 
- "Why didn't I get paged?" → Verify alert matches conditions AND check escalation chain 
- "Too many alerts" → Review if conditions too broad, add instance filters 

**Workflow:** Use "list\_alert\_rules" to find ruleId, then use this tool to review complete matching logic and routing. 

**Related tools:** "list\_alert\_rules" (find rules), "update\_alert\_rule" (modify), "get\_escalation\_chain" (check notification chain).
  - This module wraps the MCP tool 'get_alert_rule'.
  - Requires the C(ansible.mcp) collection and an MCP server connection.
version_added: "1.0.0"
author:
  - Auto-generated by AnsibleClaw
options:
  ruleId:
    description:
      - The ID of the alert rule to retrieve
    type: float
    required: true
  fields:
    description:
      - Comma-separated list of fields to include in response. Examples: "id,displayName,hostStatus" or use "*" for all fields. Omit this parameter to receive a curated set of commonly used fields.
    type: str
    required: false
seealso:
  - module: ansible.mcp.run_tool
    description: Generic MCP tool runner this module delegates to.
requirements:
  - ansible.mcp collection installed
  - MCP server accessible via ansible.mcp.mcp connection
"""

EXAMPLES = r"""
---
- name: Get detailed information about a specific alert rule by ID in LogicMonitor (LM) monitoring. 

**Returns:** Complete alert rule details: name, priority, enabled status, detailed matching conditions (device groups, datasources, datapoints, instance filters, severity levels), escalation chain assignment, suppression windows, notification settings. 

**When to use:**
- Review exact matching logic before modifying rule
- Troubleshoot why alert matched (or didn't match) this rule
- Document alert routing policies
- Verify suppression settings
- Check which escalation chain receives matching alerts


**Matching conditions explained:** 
- deviceGroups: Which resource/device folders this rule applies to (e.g., /Production/, /Database Servers/) 
- datasources: Which datasources trigger this rule (e.g., CPU, Memory, AWS_EC2) 
- datapoints: Specific metrics (e.g., CPUBusyPercent, MemoryUsedPercent) 
- instances: Filter by instance name (e.g., C: drive only, eth0 interface only) 
- severity: Alert levels (critical, error, warn) 
- escalatingChainId: Where matching alerts are routed 

**Troubleshooting use cases:** 
- "Why did this CPU alert go to wrong team?" → Check resource/device group + datasource filters 
- "Why didn't I get paged?" → Verify alert matches conditions AND check escalation chain 
- "Too many alerts" → Review if conditions too broad, add instance filters 

**Workflow:** Use "list\_alert\_rules" to find ruleId, then use this tool to review complete matching logic and routing. 

**Related tools:** "list\_alert\_rules" (find rules), "update\_alert\_rule" (modify), "get\_escalation\_chain" (check notification chain).
  hosts: mcp_servers
  connection: ansible.mcp.mcp
  gather_facts: false
  vars:
    ansible_mcp_server_name: "CHANGEME"
  tasks:
    - name: Get detailed information about a specific alert rule by ID in LogicMonitor (LM) monitoring. 

**Returns:** Complete alert rule details: name, priority, enabled status, detailed matching conditions (device groups, datasources, datapoints, instance filters, severity levels), escalation chain assignment, suppression windows, notification settings. 

**When to use:**
- Review exact matching logic before modifying rule
- Troubleshoot why alert matched (or didn't match) this rule
- Document alert routing policies
- Verify suppression settings
- Check which escalation chain receives matching alerts


**Matching conditions explained:** 
- deviceGroups: Which resource/device folders this rule applies to (e.g., /Production/, /Database Servers/) 
- datasources: Which datasources trigger this rule (e.g., CPU, Memory, AWS_EC2) 
- datapoints: Specific metrics (e.g., CPUBusyPercent, MemoryUsedPercent) 
- instances: Filter by instance name (e.g., C: drive only, eth0 interface only) 
- severity: Alert levels (critical, error, warn) 
- escalatingChainId: Where matching alerts are routed 

**Troubleshooting use cases:** 
- "Why did this CPU alert go to wrong team?" → Check resource/device group + datasource filters 
- "Why didn't I get paged?" → Verify alert matches conditions AND check escalation chain 
- "Too many alerts" → Review if conditions too broad, add instance filters 

**Workflow:** Use "list\_alert\_rules" to find ruleId, then use this tool to review complete matching logic and routing. 

**Related tools:** "list\_alert\_rules" (find rules), "update\_alert\_rule" (modify), "get\_escalation\_chain" (check notification chain).
      leogallego.logicmonitor_mcp.get_alert_rule:
        ruleId: "CHANGEME"
      register: result
"""

RETURN = r"""
---
content:
  description: List of content blocks returned by the MCP tool.
  type: list
  returned: always
  elements: dict
is_error:
  description: Whether the MCP tool reported an error.
  type: bool
  returned: always
structured_content:
  description: Optional structured result data from the MCP tool.
  type: dict
  returned: when available
tool_name:
  description: Name of the MCP tool that was called.
  type: str
  returned: always
tool_classification:
  description: Tool classification information (read_only, destructive).
  type: dict
  returned: always
"""


MCP_TOOL_NAME = "get_alert_rule"


from ansible.module_utils.basic import AnsibleModule


def main():
    module = AnsibleModule(
        argument_spec=dict(
            ruleId=dict(
                type="float",
                required=True,
            ),
            fields=dict(
                type="str",
            ),
        ),
        supports_check_mode=True,
    )

    if module.check_mode:
        module.exit_json(changed=False, msg="Check mode: no MCP call made")

    tool_args = {k: v for k, v in module.params.items() if v is not None}

    module.exit_json(
        changed=False,
        _mcp_delegate=dict(
            name=MCP_TOOL_NAME,
            args=tool_args,
        ),
    )


if __name__ == "__main__":
    main()
