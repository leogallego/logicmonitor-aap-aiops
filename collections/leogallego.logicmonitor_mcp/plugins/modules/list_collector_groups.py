#!/usr/bin/python
# -*- coding: utf-8 -*-
# Auto-generated by AnsibleClaw from MCP tool: list_collector_groups
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)

from __future__ import absolute_import, annotations, division, print_function

__metaclass__ = type


DOCUMENTATION = r"""
---
module: list_collector_groups
short_description: List all collector groups (folders) in LogicMonitor (LM) monitoring. 

**Returns:** Array of collector groups with: id, name, parentId, full path, description, number of collectors, number of subgroups. 

**What are collector groups:** Organizational folders for collectors (monitoring agents), similar to resource/device groups. Used to categorize collectors by location, function, or customer. 

**When to use:**
- Browse collector organization
- Find group IDs for collector operations
- Understand collector deployment structure
- Navigate to specific collector folders


**Common organization patterns:** 
- By location: "US-West Collectors", "EU Collectors", "APAC Collectors" 
- By environment: "Production Collectors", "Dev/Test Collectors" 
- By customer: "Customer A Collectors", "Customer B Collectors" (MSP) 
- By datacenter: "DC1 Collectors", "DC2 Collectors", "AWS Collectors" 
- By function: "Network Collectors", "Server Collectors", "Cloud Collectors" 

**Use cases:** 
- Organize collectors by geographic region 
- Group collectors by customer or tenant 
- Separate production vs non-production collectors 
- Structure multi-datacenter collector deployments 

**Workflow:** Use this tool to browse hierarchy, then "list\_collectors" filtered by groupId to see collectors in specific folder. 

**Important:** A negative "total" value in the response indicates incomplete results. Use pagination (size/offset parameters) or set autoPaginate: true to retrieve all items. 

**Related tools:** "get\_collector\_group" (details), "list\_collectors" (collectors in group), "create\_collector\_group" (create folder).
description:
  - List all collector groups (folders) in LogicMonitor (LM) monitoring. 

**Returns:** Array of collector groups with: id, name, parentId, full path, description, number of collectors, number of subgroups. 

**What are collector groups:** Organizational folders for collectors (monitoring agents), similar to resource/device groups. Used to categorize collectors by location, function, or customer. 

**When to use:**
- Browse collector organization
- Find group IDs for collector operations
- Understand collector deployment structure
- Navigate to specific collector folders


**Common organization patterns:** 
- By location: "US-West Collectors", "EU Collectors", "APAC Collectors" 
- By environment: "Production Collectors", "Dev/Test Collectors" 
- By customer: "Customer A Collectors", "Customer B Collectors" (MSP) 
- By datacenter: "DC1 Collectors", "DC2 Collectors", "AWS Collectors" 
- By function: "Network Collectors", "Server Collectors", "Cloud Collectors" 

**Use cases:** 
- Organize collectors by geographic region 
- Group collectors by customer or tenant 
- Separate production vs non-production collectors 
- Structure multi-datacenter collector deployments 

**Workflow:** Use this tool to browse hierarchy, then "list\_collectors" filtered by groupId to see collectors in specific folder. 

**Important:** A negative "total" value in the response indicates incomplete results. Use pagination (size/offset parameters) or set autoPaginate: true to retrieve all items. 

**Related tools:** "get\_collector\_group" (details), "list\_collectors" (collectors in group), "create\_collector\_group" (create folder).
  - This module wraps the MCP tool 'list_collector_groups'.
  - Requires the C(ansible.mcp) collection and an MCP server connection.
version_added: "1.0.0"
author:
  - Auto-generated by AnsibleClaw
options:
  autoPaginate:
    description:
      - Automatically fetch all pages (default: false). When true, fetches all results across multiple pages. When false, returns only the requested page. Use false for large result sets to avoid long response times.
    type: bool
    required: false
  fields:
    description:
      - Comma-separated list of fields to include in response. Examples: "id,displayName,hostStatus" or use "*" for all fields. Omit this parameter to receive a curated set of commonly used fields.
    type: str
    required: false
  filter:
    description:
      - Filter expression using LogicMonitor query syntax. Examples: name:*prod*, displayName~*server*, id>100, hostStatus:normal. Available operators: : (equals), ~ (includes), !: (not equals), !~ (not includes), >: (greater than or equals), <: (less than or equals), > (greater than), < (less than). Multiple conditions: Use comma (,) for AND, use || for OR. Do NOT use &&.
    type: str
    required: false
  offset:
    description:
      - Starting offset for pagination (default: 0). Use this to skip a specific number of results.
    type: float
    required: false
  size:
    description:
      - Number of results per page (default: 50, max: 1000).
    type: float
    required: false
seealso:
  - module: ansible.mcp.run_tool
    description: Generic MCP tool runner this module delegates to.
requirements:
  - ansible.mcp collection installed
  - MCP server accessible via ansible.mcp.mcp connection
"""

EXAMPLES = r"""
---
- name: List all collector groups (folders) in LogicMonitor (LM) monitoring. 

**Returns:** Array of collector groups with: id, name, parentId, full path, description, number of collectors, number of subgroups. 

**What are collector groups:** Organizational folders for collectors (monitoring agents), similar to resource/device groups. Used to categorize collectors by location, function, or customer. 

**When to use:**
- Browse collector organization
- Find group IDs for collector operations
- Understand collector deployment structure
- Navigate to specific collector folders


**Common organization patterns:** 
- By location: "US-West Collectors", "EU Collectors", "APAC Collectors" 
- By environment: "Production Collectors", "Dev/Test Collectors" 
- By customer: "Customer A Collectors", "Customer B Collectors" (MSP) 
- By datacenter: "DC1 Collectors", "DC2 Collectors", "AWS Collectors" 
- By function: "Network Collectors", "Server Collectors", "Cloud Collectors" 

**Use cases:** 
- Organize collectors by geographic region 
- Group collectors by customer or tenant 
- Separate production vs non-production collectors 
- Structure multi-datacenter collector deployments 

**Workflow:** Use this tool to browse hierarchy, then "list\_collectors" filtered by groupId to see collectors in specific folder. 

**Important:** A negative "total" value in the response indicates incomplete results. Use pagination (size/offset parameters) or set autoPaginate: true to retrieve all items. 

**Related tools:** "get\_collector\_group" (details), "list\_collectors" (collectors in group), "create\_collector\_group" (create folder).
  hosts: mcp_servers
  connection: ansible.mcp.mcp
  gather_facts: false
  vars:
    ansible_mcp_server_name: "CHANGEME"
  tasks:
    - name: List all collector groups (folders) in LogicMonitor (LM) monitoring. 

**Returns:** Array of collector groups with: id, name, parentId, full path, description, number of collectors, number of subgroups. 

**What are collector groups:** Organizational folders for collectors (monitoring agents), similar to resource/device groups. Used to categorize collectors by location, function, or customer. 

**When to use:**
- Browse collector organization
- Find group IDs for collector operations
- Understand collector deployment structure
- Navigate to specific collector folders


**Common organization patterns:** 
- By location: "US-West Collectors", "EU Collectors", "APAC Collectors" 
- By environment: "Production Collectors", "Dev/Test Collectors" 
- By customer: "Customer A Collectors", "Customer B Collectors" (MSP) 
- By datacenter: "DC1 Collectors", "DC2 Collectors", "AWS Collectors" 
- By function: "Network Collectors", "Server Collectors", "Cloud Collectors" 

**Use cases:** 
- Organize collectors by geographic region 
- Group collectors by customer or tenant 
- Separate production vs non-production collectors 
- Structure multi-datacenter collector deployments 

**Workflow:** Use this tool to browse hierarchy, then "list\_collectors" filtered by groupId to see collectors in specific folder. 

**Important:** A negative "total" value in the response indicates incomplete results. Use pagination (size/offset parameters) or set autoPaginate: true to retrieve all items. 

**Related tools:** "get\_collector\_group" (details), "list\_collectors" (collectors in group), "create\_collector\_group" (create folder).
      leogallego.logicmonitor_mcp.list_collector_groups:
      register: result
"""

RETURN = r"""
---
content:
  description: List of content blocks returned by the MCP tool.
  type: list
  returned: always
  elements: dict
is_error:
  description: Whether the MCP tool reported an error.
  type: bool
  returned: always
structured_content:
  description: Optional structured result data from the MCP tool.
  type: dict
  returned: when available
tool_name:
  description: Name of the MCP tool that was called.
  type: str
  returned: always
tool_classification:
  description: Tool classification information (read_only, destructive).
  type: dict
  returned: always
"""


MCP_TOOL_NAME = "list_collector_groups"


from ansible.module_utils.basic import AnsibleModule


def main():
    module = AnsibleModule(
        argument_spec=dict(
            autoPaginate=dict(
                type="bool",
            ),
            fields=dict(
                type="str",
            ),
            filter=dict(
                type="str",
            ),
            offset=dict(
                type="float",
            ),
            size=dict(
                type="float",
            ),
        ),
        supports_check_mode=True,
    )

    if module.check_mode:
        module.exit_json(changed=False, msg="Check mode: no MCP call made")

    tool_args = {k: v for k, v in module.params.items() if v is not None}

    module.exit_json(
        changed=False,
        _mcp_delegate=dict(
            name=MCP_TOOL_NAME,
            args=tool_args,
        ),
    )


if __name__ == "__main__":
    main()
