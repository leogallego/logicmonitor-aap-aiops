#!/usr/bin/python
# -*- coding: utf-8 -*-
# Auto-generated by AnsibleClaw from MCP tool: list_website_checkpoints
# GNU General Public License v3.0+ (see COPYING or https://www.gnu.org/licenses/gpl-3.0.txt)

from __future__ import absolute_import, annotations, division, print_function

__metaclass__ = type


DOCUMENTATION = r"""
---
module: list_website_checkpoints
short_description: List available checkpoint locations for website monitoring in LogicMonitor (LM) monitoring. 

**Returns:** Array of checkpoint locations with: id, name, geographic region, status, type (internal/external). 

**What are checkpoints:** Global testing locations from which LogicMonitor runs synthetic website checks. Think "test my website from New York, London, Tokyo" - checkpoints are those global vantage points. 

**When to use:**
- Check available checkpoint locations before creating website monitors
- Verify geographic coverage for multi-region testing
- Select appropriate locations for SLA monitoring
- Plan website monitoring strategy


**Checkpoint types:** 
- **External (Cloud):** LogicMonitor-managed locations around the world (US-East, EU-West, Asia-Pacific, etc.) 
- **Internal (Collector-based):** Tests run from your own collectors (test internal apps, VPNs, private networks) 

**Common checkpoint locations:** 
- North America: US-East, US-West, US-Central, Canada 
- Europe: EU-West (Ireland), EU-Central (Frankfurt), UK 
- Asia-Pacific: Singapore, Sydney, Tokyo 
- South America: São Paulo 

**Use cases:** 
- **Global SLA monitoring:** Test from regions where customers are located 
- **CDN verification:** Ensure content delivery works worldwide 
- **Regional compliance:** Monitor from specific geographic locations 
- **Multi-region performance:** Compare response times across locations 
- **Failover testing:** Verify DR sites accessible from all regions 

**Best practices:** 
- Select checkpoints near your user base 
- Use multiple checkpoints for critical services (avoid false positives from single location issues) 
- Mix internal and external checkpoints for comprehensive coverage 
- Consider timezone differences for result interpretation 

**Workflow:** Use this tool to discover available locations, then use those checkpoint IDs when creating website monitors via "create\_website". 

**Related tools:** "list\_websites" (existing monitors), "create\_website" (configure checkpoints), "get\_website" (verify checkpoint configuration).
description:
  - List available checkpoint locations for website monitoring in LogicMonitor (LM) monitoring. 

**Returns:** Array of checkpoint locations with: id, name, geographic region, status, type (internal/external). 

**What are checkpoints:** Global testing locations from which LogicMonitor runs synthetic website checks. Think "test my website from New York, London, Tokyo" - checkpoints are those global vantage points. 

**When to use:**
- Check available checkpoint locations before creating website monitors
- Verify geographic coverage for multi-region testing
- Select appropriate locations for SLA monitoring
- Plan website monitoring strategy


**Checkpoint types:** 
- **External (Cloud):** LogicMonitor-managed locations around the world (US-East, EU-West, Asia-Pacific, etc.) 
- **Internal (Collector-based):** Tests run from your own collectors (test internal apps, VPNs, private networks) 

**Common checkpoint locations:** 
- North America: US-East, US-West, US-Central, Canada 
- Europe: EU-West (Ireland), EU-Central (Frankfurt), UK 
- Asia-Pacific: Singapore, Sydney, Tokyo 
- South America: São Paulo 

**Use cases:** 
- **Global SLA monitoring:** Test from regions where customers are located 
- **CDN verification:** Ensure content delivery works worldwide 
- **Regional compliance:** Monitor from specific geographic locations 
- **Multi-region performance:** Compare response times across locations 
- **Failover testing:** Verify DR sites accessible from all regions 

**Best practices:** 
- Select checkpoints near your user base 
- Use multiple checkpoints for critical services (avoid false positives from single location issues) 
- Mix internal and external checkpoints for comprehensive coverage 
- Consider timezone differences for result interpretation 

**Workflow:** Use this tool to discover available locations, then use those checkpoint IDs when creating website monitors via "create\_website". 

**Related tools:** "list\_websites" (existing monitors), "create\_website" (configure checkpoints), "get\_website" (verify checkpoint configuration).
  - This module wraps the MCP tool 'list_website_checkpoints'.
  - Requires the C(ansible.mcp) collection and an MCP server connection.
version_added: "1.0.0"
author:
  - Auto-generated by AnsibleClaw
options:
  fields:
    description:
      - Comma-separated list of fields to include in response. Examples: "id,displayName,hostStatus" or use "*" for all fields. Omit this parameter to receive a curated set of commonly used fields.
    type: str
    required: false
seealso:
  - module: ansible.mcp.run_tool
    description: Generic MCP tool runner this module delegates to.
requirements:
  - ansible.mcp collection installed
  - MCP server accessible via ansible.mcp.mcp connection
"""

EXAMPLES = r"""
---
- name: List available checkpoint locations for website monitoring in LogicMonitor (LM) monitoring. 

**Returns:** Array of checkpoint locations with: id, name, geographic region, status, type (internal/external). 

**What are checkpoints:** Global testing locations from which LogicMonitor runs synthetic website checks. Think "test my website from New York, London, Tokyo" - checkpoints are those global vantage points. 

**When to use:**
- Check available checkpoint locations before creating website monitors
- Verify geographic coverage for multi-region testing
- Select appropriate locations for SLA monitoring
- Plan website monitoring strategy


**Checkpoint types:** 
- **External (Cloud):** LogicMonitor-managed locations around the world (US-East, EU-West, Asia-Pacific, etc.) 
- **Internal (Collector-based):** Tests run from your own collectors (test internal apps, VPNs, private networks) 

**Common checkpoint locations:** 
- North America: US-East, US-West, US-Central, Canada 
- Europe: EU-West (Ireland), EU-Central (Frankfurt), UK 
- Asia-Pacific: Singapore, Sydney, Tokyo 
- South America: São Paulo 

**Use cases:** 
- **Global SLA monitoring:** Test from regions where customers are located 
- **CDN verification:** Ensure content delivery works worldwide 
- **Regional compliance:** Monitor from specific geographic locations 
- **Multi-region performance:** Compare response times across locations 
- **Failover testing:** Verify DR sites accessible from all regions 

**Best practices:** 
- Select checkpoints near your user base 
- Use multiple checkpoints for critical services (avoid false positives from single location issues) 
- Mix internal and external checkpoints for comprehensive coverage 
- Consider timezone differences for result interpretation 

**Workflow:** Use this tool to discover available locations, then use those checkpoint IDs when creating website monitors via "create\_website". 

**Related tools:** "list\_websites" (existing monitors), "create\_website" (configure checkpoints), "get\_website" (verify checkpoint configuration).
  hosts: mcp_servers
  connection: ansible.mcp.mcp
  gather_facts: false
  vars:
    ansible_mcp_server_name: "CHANGEME"
  tasks:
    - name: List available checkpoint locations for website monitoring in LogicMonitor (LM) monitoring. 

**Returns:** Array of checkpoint locations with: id, name, geographic region, status, type (internal/external). 

**What are checkpoints:** Global testing locations from which LogicMonitor runs synthetic website checks. Think "test my website from New York, London, Tokyo" - checkpoints are those global vantage points. 

**When to use:**
- Check available checkpoint locations before creating website monitors
- Verify geographic coverage for multi-region testing
- Select appropriate locations for SLA monitoring
- Plan website monitoring strategy


**Checkpoint types:** 
- **External (Cloud):** LogicMonitor-managed locations around the world (US-East, EU-West, Asia-Pacific, etc.) 
- **Internal (Collector-based):** Tests run from your own collectors (test internal apps, VPNs, private networks) 

**Common checkpoint locations:** 
- North America: US-East, US-West, US-Central, Canada 
- Europe: EU-West (Ireland), EU-Central (Frankfurt), UK 
- Asia-Pacific: Singapore, Sydney, Tokyo 
- South America: São Paulo 

**Use cases:** 
- **Global SLA monitoring:** Test from regions where customers are located 
- **CDN verification:** Ensure content delivery works worldwide 
- **Regional compliance:** Monitor from specific geographic locations 
- **Multi-region performance:** Compare response times across locations 
- **Failover testing:** Verify DR sites accessible from all regions 

**Best practices:** 
- Select checkpoints near your user base 
- Use multiple checkpoints for critical services (avoid false positives from single location issues) 
- Mix internal and external checkpoints for comprehensive coverage 
- Consider timezone differences for result interpretation 

**Workflow:** Use this tool to discover available locations, then use those checkpoint IDs when creating website monitors via "create\_website". 

**Related tools:** "list\_websites" (existing monitors), "create\_website" (configure checkpoints), "get\_website" (verify checkpoint configuration).
      leogallego.logicmonitor_mcp.list_website_checkpoints:
      register: result
"""

RETURN = r"""
---
content:
  description: List of content blocks returned by the MCP tool.
  type: list
  returned: always
  elements: dict
is_error:
  description: Whether the MCP tool reported an error.
  type: bool
  returned: always
structured_content:
  description: Optional structured result data from the MCP tool.
  type: dict
  returned: when available
tool_name:
  description: Name of the MCP tool that was called.
  type: str
  returned: always
tool_classification:
  description: Tool classification information (read_only, destructive).
  type: dict
  returned: always
"""


MCP_TOOL_NAME = "list_website_checkpoints"


from ansible.module_utils.basic import AnsibleModule


def main():
    module = AnsibleModule(
        argument_spec=dict(
            fields=dict(
                type="str",
            ),
        ),
        supports_check_mode=True,
    )

    if module.check_mode:
        module.exit_json(changed=False, msg="Check mode: no MCP call made")

    tool_args = {k: v for k, v in module.params.items() if v is not None}

    module.exit_json(
        changed=False,
        _mcp_delegate=dict(
            name=MCP_TOOL_NAME,
            args=tool_args,
        ),
    )


if __name__ == "__main__":
    main()
